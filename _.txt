_learn
    notify function
    find out why ```
        export class EscapedEmail {
            value: string;
            constructor(email: string) {
                this.value = escape_email(email);
            }
        }

        const email = new EscapedEmail("example@email.com")

        const path = `$.users.${email}`
    ``` works
    typescript - combine two types
    get the keys of a typescript interface into an Array?
    how typescript extends works
    what a boolean looks like in a stored redis json value and how to filter by it in ft.search
    what happens - redis - get - multiple paths
    paystack charge.success event body.data.paid_at value
    redis precise millisecond
    redis - how to filter by a boolean value in a JSON
    how to index keys of a json object as tags in Redis
    goto previous page in load function
    goto previous page in component
    javascript disabled
todo
    specific type for PostId
    check
        what a boolean looks like in a stored redis json value and how to filter by it in ft.search
    tests
        reply
    postentry requires payment indicator
    pay for post page
    pay with paystack
    markdown
    reply
    creator in PostEntry return from search
    vite-pwa
    PaystackPop window type
    auth page
        load function checks if logged in, if logged in, redirects to previous page
        button to go previous page if logged in
    users
        add_users
    members
        automatically make member upon payment?
        return false from allowed() if not member
    article type
        article
        group
    rename creator to 'creator' in all previous entries
    pay with paypal
    Search component 
        filter
            payment required only, payment not required only